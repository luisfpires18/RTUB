@page "/admin/labels"
@using Microsoft.AspNetCore.Components
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        // Redirect to the new owner/labels location
        NavigationManager.NavigateTo("/owner/labels", forceLoad: true);
    }
}
