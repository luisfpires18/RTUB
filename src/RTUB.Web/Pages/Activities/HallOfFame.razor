@page "/hall-of-fame"
@rendermode InteractiveServer
@using RTUB.Application.Interfaces
@using RTUB.Application.Helpers
@using RTUB.Application.Extensions
@using RTUB.Core.Enums
@using Microsoft.AspNetCore.Identity
@using Microsoft.AspNetCore.Authorization
@using Microsoft.EntityFrameworkCore
@inject UserManager<ApplicationUser> UserManager
@inject IRoleAssignmentService RoleAssignmentService
@inject IEnrollmentService EnrollmentService
@inject IRehearsalService RehearsalService
@inject IRehearsalAttendanceRepository RehearsalAttendanceRepository
@inject IMemberInstrumentService MemberInstrumentService
@attribute [Authorize]

<h1 class="mb-2"><i class="bi bi-trophy"></i> Hall of Fame</h1>
<p class="lead page-header-subtitle mb-4 text-light-theme">Os recordistas da RTUB - quem brilha mais em cada categoria.</p>

@if (isLoading)
{
    <LoadingSpinner Message="A carregar hall of fame..." Color="@SpinnerColor.Purple" />
}
else
{
    <!-- Row 1: Última Passagem a Caloiro & Última Passagem a Tuno -->
    <div class="hof-row">
        <div class="hof-panel">
            <section>
                <div class="card shadow-sm portal-governing-body-card">
                    <div class="card-header gov-body-card-header">
                        <h3 class="mb-0">ÚLTIMA PASSAGEM A CALOIRO</h3>
                    </div>
                    <div class="card-body">
                        @if (mostRecentCaloiro.Any())
                        {
                            <div class="row">
                                @foreach (var member in mostRecentCaloiro)
                                {
                                    <div class="col-md-@(mostRecentCaloiro.Count > 1 ? "6" : "12") text-center mb-3" @key="member.User.Id">
                                        <div class="d-flex flex-column align-items-center">
                                            <img src="@member.User.ProfilePictureSrc" alt="@member.User.GetDisplayName()" class="member-avatar-large mb-2" />
                                            @if (!string.IsNullOrEmpty(member.User.Nickname))
                                            {
                                                <div class="text-light-theme">@member.User.Nickname</div>
                                                <div class="member-nickname">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            else
                                            {
                                                <div class="text-light-theme">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            <div class="text-muted small mt-1">@FormatMonthYear(member.Date)</div>
                                        </div>
                                    </div>
                                }
                            </div>
                        }
                        else
                        {
                            <div class="not-available-text">Sem dados suficientes</div>
                        }
                    </div>
                </div>
            </section>
        </div>
        <div class="hof-panel">
            <section>
                <div class="card shadow-sm portal-governing-body-card">
                    <div class="card-header gov-body-card-header">
                        <h3 class="mb-0">ÚLTIMA PASSAGEM A TUNO</h3>
                    </div>
                    <div class="card-body">
                        @if (mostRecentTuno.Any())
                        {
                            <div class="row">
                                @foreach (var member in mostRecentTuno)
                                {
                                    <div class="col-md-@(mostRecentTuno.Count > 1 ? "6" : "12") text-center mb-3" @key="member.User.Id">
                                        <div class="d-flex flex-column align-items-center">
                                            <img src="@member.User.ProfilePictureSrc" alt="@member.User.GetDisplayName()" class="member-avatar-large mb-2" />
                                            @if (!string.IsNullOrEmpty(member.User.Nickname))
                                            {
                                                <div class="text-light-theme">@member.User.Nickname</div>
                                                <div class="member-nickname">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            else
                                            {
                                                <div class="text-light-theme">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            <div class="text-muted small mt-1">@FormatMonthYear(member.Date)</div>
                                        </div>
                                    </div>
                                }
                            </div>
                        }
                        else
                        {
                            <div class="not-available-text">Sem dados suficientes</div>
                        }
                    </div>
                </div>
            </section>
        </div>
    </div>

    <!-- Row 2: Afilhados & Cargos -->
    <div class="hof-row">
        <div class="hof-panel">
            <section>
                <div class="card shadow-sm portal-governing-body-card">
                    <div class="card-header gov-body-card-header">
                        <h3 class="mb-0">MAIS AFILHADOS</h3>
                    </div>
                    <div class="card-body">
                        @if (mostAfilhados.Any())
                        {
                            <div class="row">
                                @foreach (var member in mostAfilhados)
                                {
                                    <div class="col-md-@(mostAfilhados.Count > 1 ? "6" : "12") text-center mb-3">
                                        <div class="d-flex flex-column align-items-center">
                                            <img src="@member.User.ProfilePictureSrc" alt="@member.User.GetDisplayName()" class="member-avatar-large mb-2" />
                                            @if (!string.IsNullOrEmpty(member.User.Nickname))
                                            {
                                                <div class="text-light-theme">@member.User.Nickname</div>
                                                <div class="member-nickname">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            else
                                            {
                                                <div class="text-light-theme">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            <div class="text-muted small mt-1">@member.Count @(member.Count == 1 ? "afilhado" : "afilhados")</div>
                                        </div>
                                    </div>
                                }
                            </div>
                        }
                        else
                        {
                            <div class="not-available-text">Sem dados suficientes</div>
                        }
                    </div>
                </div>
            </section>
        </div>
        <div class="hof-panel">
            <section>
                <div class="card shadow-sm portal-governing-body-card">
                    <div class="card-header gov-body-card-header">
                        <h3 class="mb-0">MAIS CARGOS</h3>
                    </div>
                    <div class="card-body">
                        @if (mostPositions.Any())
                        {
                            <div class="row">
                                @foreach (var member in mostPositions)
                                {
                                    <div class="col-md-@(mostPositions.Count > 1 ? "6" : "12") text-center mb-3" @key="member.User.Id">
                                        <div class="d-flex flex-column align-items-center">
                                            <img src="@member.User.ProfilePictureSrc" alt="@member.User.GetDisplayName()" class="member-avatar-large mb-2" />
                                            @if (!string.IsNullOrEmpty(member.User.Nickname))
                                            {
                                                <div class="text-light-theme">@member.User.Nickname</div>
                                                <div class="member-nickname">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            else
                                            {
                                                <div class="text-light-theme">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            <div class="text-muted small mt-1">@member.Count @(member.Count == 1 ? "cargo" : "cargos")</div>
                                        </div>
                                    </div>
                                }
                            </div>
                        }
                        else
                        {
                            <div class="not-available-text">Sem dados suficientes</div>
                        }
                    </div>
                </div>
            </section>
        </div>
    </div>

    <!-- Row 3: Magister & Instrumentos -->
    <div class="hof-row">
        <div class="hof-panel">
            <section>
                <div class="card shadow-sm portal-governing-body-card">
                    <div class="card-header gov-body-card-header">
                        <h3 class="mb-0">MAIS VEZES MAGISTER</h3>
                    </div>
                    <div class="card-body">
                        @if (mostMagister.Any())
                        {
                            <div class="row">
                                @foreach (var member in mostMagister)
                                {
                                    <div class="col-md-@(mostMagister.Count > 1 ? "6" : "12") text-center mb-3" @key="member.User.Id">
                                        <div class="d-flex flex-column align-items-center">
                                            <img src="@member.User.ProfilePictureSrc" alt="@member.User.GetDisplayName()" class="member-avatar-large mb-2" />
                                            @if (!string.IsNullOrEmpty(member.User.Nickname))
                                            {
                                                <div class="text-light-theme">@member.User.Nickname</div>
                                                <div class="member-nickname">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            else
                                            {
                                                <div class="text-light-theme">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            <div class="text-muted small mt-1">@member.Count @(member.Count == 1 ? "mandato" : "mandatos")</div>
                                        </div>
                                    </div>
                                }
                            </div>
                        }
                        else
                        {
                            <div class="not-available-text">Sem dados suficientes</div>
                        }
                    </div>
                </div>
            </section>
        </div>
        <div class="hof-panel">
            <section>
                <div class="card shadow-sm portal-governing-body-card">
                    <div class="card-header gov-body-card-header">
                        <h3 class="mb-0">MAIS INSTRUMENTOS A TOCAR</h3>
                    </div>
                    <div class="card-body">
                        @if (mostInstruments.Any())
                        {
                            <div class="row">
                                @foreach (var member in mostInstruments)
                                {
                                    <div class="col-md-@(mostInstruments.Count > 1 ? "6" : "12") text-center mb-3" @key="member.User.Id">
                                        <div class="d-flex flex-column align-items-center">
                                            <img src="@member.User.ProfilePictureSrc" alt="@member.User.GetDisplayName()" class="member-avatar-large mb-2" />
                                            @if (!string.IsNullOrEmpty(member.User.Nickname))
                                            {
                                                <div class="text-light-theme">@member.User.Nickname</div>
                                                <div class="member-nickname">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            else
                                            {
                                                <div class="text-light-theme">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            <div class="text-muted small mt-1">@member.Count @(member.Count == 1 ? "instrumento" : "instrumentos")</div>
                                        </div>
                                    </div>
                                }
                            </div>
                        }
                        else
                        {
                            <div class="not-available-text">Sem dados suficientes</div>
                        }
                    </div>
                </div>
            </section>
        </div>
    </div>

    <!-- Row 4: Tempo a Leitão -->
    <div class="hof-row">
        <div class="hof-panel">
            <section>
                <div class="card shadow-sm portal-governing-body-card">
                    <div class="card-header gov-body-card-header">
                        <h3 class="mb-0">MAIS TEMPO A LEITÃO</h3>
                    </div>
                    <div class="card-body">
                        @if (longestLeitao.Any())
                        {
                            <div class="row">
                                @foreach (var member in longestLeitao)
                                {
                                    <div class="col-md-@(longestLeitao.Count > 1 ? "6" : "12") text-center mb-3">
                                        <div class="d-flex flex-column align-items-center">
                                            <img src="@member.User.ProfilePictureSrc" alt="@member.User.GetDisplayName()" class="member-avatar-large mb-2" />
                                            @if (!string.IsNullOrEmpty(member.User.Nickname))
                                            {
                                                <div class="text-light-theme">@member.User.Nickname</div>
                                                <div class="member-nickname">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            else
                                            {
                                                <div class="text-light-theme">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            <div class="text-muted small mt-1">@FormatDuration(member.Duration)</div>
                                        </div>
                                    </div>
                                }
                            </div>
                        }
                        else
                        {
                            <div class="not-available-text">Sem dados suficientes</div>
                        }
                    </div>
                </div>
            </section>
        </div>
        <div class="hof-panel">
            <section>
                <div class="card shadow-sm portal-governing-body-card">
                    <div class="card-header gov-body-card-header">
                        <h3 class="mb-0">MENOS TEMPO A LEITÃO</h3>
                    </div>
                    <div class="card-body">
                        @if (shortestLeitao.Any())
                        {
                            <div class="row">
                                @foreach (var member in shortestLeitao)
                                {
                                    <div class="col-md-@(shortestLeitao.Count > 1 ? "6" : "12") text-center mb-3" @key="member.User.Id">
                                        <div class="d-flex flex-column align-items-center">
                                            <img src="@member.User.ProfilePictureSrc" alt="@member.User.GetDisplayName()" class="member-avatar-large mb-2" />
                                            @if (!string.IsNullOrEmpty(member.User.Nickname))
                                            {
                                                <div class="text-light-theme">@member.User.Nickname</div>
                                                <div class="member-nickname">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            else
                                            {
                                                <div class="text-light-theme">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            <div class="text-muted small mt-1">@FormatDuration(member.Duration)</div>
                                        </div>
                                    </div>
                                }
                            </div>
                        }
                        else
                        {
                            <div class="not-available-text">Sem dados suficientes</div>
                        }
                    </div>
                </div>
            </section>
        </div>
    </div>

    <!-- Row 5: Tempo a Caloiro -->
    <div class="hof-row">
        <div class="hof-panel">
            <section>
                <div class="card shadow-sm portal-governing-body-card">
                    <div class="card-header gov-body-card-header">
                        <h3 class="mb-0">MAIS TEMPO A CALOIRO</h3>
                    </div>
                    <div class="card-body">
                        @if (longestCaloiro.Any())
                        {
                            <div class="row">
                                @foreach (var member in longestCaloiro)
                                {
                                    <div class="col-md-@(longestCaloiro.Count > 1 ? "6" : "12") text-center mb-3" @key="member.User.Id">
                                        <div class="d-flex flex-column align-items-center">
                                            <img src="@member.User.ProfilePictureSrc" alt="@member.User.GetDisplayName()" class="member-avatar-large mb-2" />
                                            @if (!string.IsNullOrEmpty(member.User.Nickname))
                                            {
                                                <div class="text-light-theme">@member.User.Nickname</div>
                                                <div class="member-nickname">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            else
                                            {
                                                <div class="text-light-theme">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            <div class="text-muted small mt-1">@FormatDuration(member.Duration)</div>
                                        </div>
                                    </div>
                                }
                            </div>
                        }
                        else
                        {
                            <div class="not-available-text">Sem dados suficientes</div>
                        }
                    </div>
                </div>
            </section>
        </div>
        <div class="hof-panel">
            <section>
                <div class="card shadow-sm portal-governing-body-card">
                    <div class="card-header gov-body-card-header">
                        <h3 class="mb-0">MENOS TEMPO A CALOIRO</h3>
                    </div>
                    <div class="card-body">
                        @if (shortestCaloiro.Any())
                        {
                            <div class="row">
                                @foreach (var member in shortestCaloiro)
                                {
                                    <div class="col-md-@(shortestCaloiro.Count > 1 ? "6" : "12") text-center mb-3">
                                        <div class="d-flex flex-column align-items-center">
                                            <img src="@member.User.ProfilePictureSrc" alt="@member.User.GetDisplayName()" class="member-avatar-large mb-2" />
                                            @if (!string.IsNullOrEmpty(member.User.Nickname))
                                            {
                                                <div class="text-light-theme">@member.User.Nickname</div>
                                                <div class="member-nickname">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            else
                                            {
                                                <div class="text-light-theme">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            <div class="text-muted small mt-1">@FormatDuration(member.Duration)</div>
                                        </div>
                                    </div>
                                }
                            </div>
                        }
                        else
                        {
                            <div class="not-available-text">Sem dados suficientes</div>
                        }
                    </div>
                </div>
            </section>
        </div>
    </div>

    <!-- Row 6: Ensaios & Atuações -->
    <div class="hof-row">
        <div class="hof-panel">
            <section>
                <div class="card shadow-sm portal-governing-body-card">
                    <div class="card-header gov-body-card-header">
                        <h3 class="mb-0">MAIS ENSAIOS</h3>
                    </div>
                    <div class="card-body">
                        @if (mostRehearsals.Any())
                        {
                            <div class="row">
                                @foreach (var member in mostRehearsals)
                                {
                                    <div class="col-md-@(mostRehearsals.Count > 1 ? "6" : "12") text-center mb-3" @key="member.User.Id">
                                        <div class="d-flex flex-column align-items-center">
                                            <img src="@member.User.ProfilePictureSrc" alt="@member.User.GetDisplayName()" class="member-avatar-large mb-2" />
                                            @if (!string.IsNullOrEmpty(member.User.Nickname))
                                            {
                                                <div class="text-light-theme">@member.User.Nickname</div>
                                                <div class="member-nickname">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            else
                                            {
                                                <div class="text-light-theme">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            <div class="text-muted small mt-1">@member.Count @(member.Count == 1 ? "ensaio" : "ensaios")</div>
                                        </div>
                                    </div>
                                }
                            </div>
                        }
                        else
                        {
                            <div class="not-available-text">Sem dados suficientes</div>
                        }
                    </div>
                </div>
            </section>
        </div>
        <div class="hof-panel">
            <section>
                <div class="card shadow-sm portal-governing-body-card">
                    <div class="card-header gov-body-card-header">
                        <h3 class="mb-0">MAIS ATUAÇÕES</h3>
                    </div>
                    <div class="card-body">
                        @if (mostPerformances.Any())
                        {
                            <div class="row">
                                @foreach (var member in mostPerformances)
                                {
                                    <div class="col-md-@(mostPerformances.Count > 1 ? "6" : "12") text-center mb-3" @key="member.User.Id">
                                        <div class="d-flex flex-column align-items-center">
                                            <img src="@member.User.ProfilePictureSrc" alt="@member.User.GetDisplayName()" class="member-avatar-large mb-2" />
                                            @if (!string.IsNullOrEmpty(member.User.Nickname))
                                            {
                                                <div class="text-light-theme">@member.User.Nickname</div>
                                                <div class="member-nickname">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            else
                                            {
                                                <div class="text-light-theme">@member.User.FirstName @member.User.LastName</div>
                                            }
                                            <div class="text-muted small mt-1">@member.Count @(member.Count == 1 ? "atuação" : "atuações")</div>
                                        </div>
                                    </div>
                                }
                            </div>
                        }
                        else
                        {
                            <div class="not-available-text">Sem dados suficientes</div>
                        }
                    </div>
                </div>
            </section>
        </div>
    </div>
}
@code {
    private bool isLoading = true;

    // Data models for metrics
    private class MemberDuration
    {
        public ApplicationUser User { get; set; } = null!;
        public int Duration { get; set; } // Duration in months
    }

    private class MemberCount
    {
        public ApplicationUser User { get; set; } = null!;
        public int Count { get; set; }
    }

    private class MemberDate
    {
        public ApplicationUser User { get; set; } = null!;
        public DateTime Date { get; set; }
    }

    // Metric results
    private List<MemberDate> mostRecentCaloiro = new();
    private List<MemberDate> mostRecentTuno = new();
    private List<MemberDuration> longestCaloiro = new();
    private List<MemberDuration> shortestCaloiro = new();
    private List<MemberDuration> longestLeitao = new();
    private List<MemberDuration> shortestLeitao = new();
    private List<MemberCount> mostPositions = new();
    private List<MemberCount> mostMagister = new();
    private List<MemberCount> mostRehearsals = new();
    private List<MemberCount> mostPerformances = new();
    private List<MemberCount> mostAfilhados = new();
    private List<MemberCount> mostInstruments = new();

    protected override async Task OnInitializedAsync()
    {
        await LoadData();
        isLoading = false;
    }

    private async Task LoadData()
    {
        // Get all users
        var allUsers = await UserManager.Users.AsNoTracking().ToListAsync();

        // Get all role assignments
        var allRoleAssignments = await RoleAssignmentService.GetAllRoleAssignmentsAsync();

        // Build user dictionary for efficient lookup (avoid N+1)
        var usersDict = allUsers.ToDictionary(u => u.Id);

        // Assign users to role assignments
        foreach (var assignment in allRoleAssignments)
        {
            if (usersDict.TryGetValue(assignment.UserId, out var user))
            {
                assignment.User = user;
            }
        }

        // Calculate metrics
        await CalculateMostRecentCaloiro(allUsers);
        await CalculateMostRecentTuno(allUsers);
        await CalculateCaloiroDurations(allUsers);
        await CalculateLeitaoDurations(allUsers);
        await CalculateMostPositions(allUsers, allRoleAssignments.ToList());
        await CalculateMostMagister(allUsers, allRoleAssignments.ToList());
        await CalculateMostRehearsals(allUsers);
        await CalculateMostPerformances(allUsers);
        await CalculateMostAfilhados(allUsers);
        await CalculateMostInstruments(allUsers);
    }

    private async Task CalculateMostRecentCaloiro(List<ApplicationUser> allUsers)
    {
        // Find users who have valid YearCaloiro and MonthCaloiro
        // Create date from YearCaloiro and MonthCaloiro
        // Find the most recent date(s) and the user(s) with that date
        var usersWithDates = allUsers
            .Where(u => u.YearCaloiro.HasValue && u.MonthCaloiro.HasValue
                     && u.YearCaloiro.Value >= 1 && u.YearCaloiro.Value <= 9999
                     && u.MonthCaloiro.Value >= 1 && u.MonthCaloiro.Value <= 12)
            .Select(u => new MemberDate
            {
                User = u,
                Date = new DateTime(u.YearCaloiro!.Value, u.MonthCaloiro!.Value, 1)
            })
            .ToList();

        if (usersWithDates.Any())
        {
            var maxDate = usersWithDates.Max(x => x.Date);
            mostRecentCaloiro = usersWithDates.Where(x => x.Date == maxDate).ToList();
        }

        await Task.CompletedTask;
    }

    private async Task CalculateMostRecentTuno(List<ApplicationUser> allUsers)
    {
        // Find users who have valid YearTuno and MonthTuno
        // Create date from YearTuno and MonthTuno
        // Find the most recent date(s) and the user(s) with that date
        var usersWithDates = allUsers
            .Where(u => u.YearTuno.HasValue && u.MonthTuno.HasValue
                     && u.YearTuno.Value >= 1 && u.YearTuno.Value <= 9999
                     && u.MonthTuno.Value >= 1 && u.MonthTuno.Value <= 12)
            .Select(u => new MemberDate
            {
                User = u,
                Date = new DateTime(u.YearTuno!.Value, u.MonthTuno!.Value, 1)
            })
            .ToList();

        if (usersWithDates.Any())
        {
            var maxDate = usersWithDates.Max(x => x.Date);
            mostRecentTuno = usersWithDates.Where(x => x.Date == maxDate).ToList();
        }

        await Task.CompletedTask;
    }

    private async Task CalculateCaloiroDurations(List<ApplicationUser> allUsers)
    {
        var durations = new List<MemberDuration>();

        foreach (var user in allUsers)
        {
            // Need both Caloiro and Tuno dates
            if (user.YearCaloiro.HasValue && user.MonthCaloiro.HasValue &&
                user.YearTuno.HasValue && user.MonthTuno.HasValue)
            {
                var caloiroDate = new DateTime(user.YearCaloiro.Value, user.MonthCaloiro.Value, 1);
                var tunoDate = new DateTime(user.YearTuno.Value, user.MonthTuno.Value, 1);

                // Calculate duration in months
                var durationMonths = ((tunoDate.Year - caloiroDate.Year) * 12) + (tunoDate.Month - caloiroDate.Month);

                if (durationMonths > 0)
                {
                    durations.Add(new MemberDuration
                    {
                        User = user,
                        Duration = durationMonths
                    });
                }
            }
        }

        if (durations.Any())
        {
            var maxDuration = durations.Max(d => d.Duration);
            longestCaloiro = durations.Where(d => d.Duration == maxDuration).ToList();

            var minDuration = durations.Min(d => d.Duration);
            shortestCaloiro = durations.Where(d => d.Duration == minDuration).ToList();
        }

        await Task.CompletedTask;
    }

    private async Task CalculateLeitaoDurations(List<ApplicationUser> allUsers)
    {
        var durations = new List<MemberDuration>();

        foreach (var user in allUsers)
        {
            // Need both Leitao and Caloiro dates
            if (user.YearLeitao.HasValue && user.MonthLeitao.HasValue &&
                user.YearCaloiro.HasValue && user.MonthCaloiro.HasValue)
            {
                var leitaoDate = new DateTime(user.YearLeitao.Value, user.MonthLeitao.Value, 1);
                var caloiroDate = new DateTime(user.YearCaloiro.Value, user.MonthCaloiro.Value, 1);

                // Calculate duration in months
                var durationMonths = ((caloiroDate.Year - leitaoDate.Year) * 12) + (caloiroDate.Month - leitaoDate.Month);

                if (durationMonths > 0)
                {
                    durations.Add(new MemberDuration
                    {
                        User = user,
                        Duration = durationMonths
                    });
                }
            }
        }

        if (durations.Any())
        {
            var maxDuration = durations.Max(d => d.Duration);
            longestLeitao = durations.Where(d => d.Duration == maxDuration).ToList();

            var minDuration = durations.Min(d => d.Duration);
            shortestLeitao = durations.Where(d => d.Duration == minDuration).ToList();
        }

        await Task.CompletedTask;
    }

    private async Task CalculateMostPositions(List<ApplicationUser> allUsers, List<RoleAssignment> allRoleAssignments)
    {
        var counts = allUsers
            .Select(user => new MemberCount
            {
                User = user,
                Count = allRoleAssignments.Count(ra => ra.UserId == user.Id)
            })
            .Where(mc => mc.Count > 0)
            .ToList();

        if (counts.Any())
        {
            var maxCount = counts.Max(c => c.Count);
            mostPositions = counts.Where(c => c.Count == maxCount).ToList();
        }

        await Task.CompletedTask;
    }

    private async Task CalculateMostMagister(List<ApplicationUser> allUsers, List<RoleAssignment> allRoleAssignments)
    {
        var counts = allUsers
            .Select(user => new MemberCount
            {
                User = user,
                Count = allRoleAssignments.Count(ra => ra.UserId == user.Id && ra.Position == Position.Magister)
            })
            .Where(mc => mc.Count > 0)
            .ToList();

        if (counts.Any())
        {
            var maxCount = counts.Max(c => c.Count);
            mostMagister = counts.Where(c => c.Count == maxCount).ToList();
        }

        await Task.CompletedTask;
    }

    private async Task CalculateMostRehearsals(List<ApplicationUser> allUsers)
    {
        // Use efficient query to count rehearsal attendances per member
        // Filter by past dates and not cancelled (matching leaderboard XP logic)
        var now = DateTime.UtcNow;

        // Build efficient query using repository - single database roundtrip
        var rehearsalCounts = await RehearsalAttendanceRepository.Query()
            .Include(ra => ra.Rehearsal)
            .Include(ra => ra.User)
            .Where(ra => ra.Rehearsal != null
                      && ra.Rehearsal.Date < now
                      && !ra.Rehearsal.IsCanceled
                      && ra.Attended)
            .GroupBy(ra => ra.UserId)
            .Select(g => new
            {
                UserId = g.Key,
                Count = g.Count()
            })
            .ToListAsync();

        // If no data at all, leave mostRehearsals empty
        if (!rehearsalCounts.Any())
        {
            mostRehearsals = new List<MemberCount>();
            return;
        }

        // Find the maximum count
        var maxCount = rehearsalCounts.Max(rc => rc.Count);

        // Get all users with the maximum count
        var topUserIds = rehearsalCounts
            .Where(rc => rc.Count == maxCount)
            .Select(rc => rc.UserId)
            .ToHashSet();

        // Map to MemberCount with User objects
        mostRehearsals = allUsers
            .Where(u => topUserIds.Contains(u.Id))
            .Select(u => new MemberCount
            {
                User = u,
                Count = maxCount
            })
            .ToList();
    }

    private async Task CalculateMostPerformances(List<ApplicationUser> allUsers)
    {
        // Get all enrollments - filter by past dates and not cancelled (matching leaderboard XP logic)
        var now = DateTime.UtcNow;
        var allEnrollments = await EnrollmentService.GetAllEnrollmentsAsync();

        // Filter to past, non-cancelled events with WillAttend = true
        var counts = allUsers
            .Select(user => new MemberCount
            {
                User = user,
                Count = allEnrollments
                    .Where(e =>
                        e.Event != null
                        && (e.Event.EndDate ?? e.Event.Date) < now
                        && !e.Event.IsCancelled)
                    .Count(e => e.UserId == user.Id && e.WillAttend)
            })
            .Where(mc => mc.Count > 0)
            .ToList();

        if (counts.Any())
        {
            var maxCount = counts.Max(c => c.Count);
            mostPerformances = counts.Where(c => c.Count == maxCount).ToList();
        }
    }

    private async Task CalculateMostAfilhados(List<ApplicationUser> allUsers)
    {
        // Count how many times a user appears as mentor for other members
        var mentorCounts = allUsers
            .Where(u => !string.IsNullOrEmpty(u.MentorId))
            .GroupBy(u => u.MentorId!)
            .Select(g => new
            {
                MentorId = g.Key,
                Count = g.Count()
            })
            .ToList();

        if (!mentorCounts.Any())
        {
            mostAfilhados = new List<MemberCount>();
            return;
        }

        var maxCount = mentorCounts.Max(mc => mc.Count);
        var topMentorIds = mentorCounts
            .Where(mc => mc.Count == maxCount)
            .Select(mc => mc.MentorId)
            .ToHashSet();

        mostAfilhados = allUsers
            .Where(u => topMentorIds.Contains(u.Id))
            .Select(u => new MemberCount
            {
                User = u,
                Count = maxCount
            })
            .ToList();

        await Task.CompletedTask;
    }

    private async Task CalculateMostInstruments(List<ApplicationUser> allUsers)
    {
        // Batch load all instruments in one query to avoid N+1
        var userIds = allUsers.Select(u => u.Id).ToList();
        var allInstrumentsByUser = await MemberInstrumentService.GetMemberInstrumentsByUserIdsAsync(userIds);

        var counts = allUsers
            .Select(user => new
            {
                User = user,
                Count = allInstrumentsByUser.GetValueOrDefault(user.Id, new List<MemberInstrument>()).Count
            })
            .Where(x => x.Count > 0)
            .Select(x => new MemberCount
            {
                User = x.User,
                Count = x.Count
            })
            .ToList();

        if (counts.Any())
        {
            var maxCount = counts.Max(c => c.Count);
            mostInstruments = counts.Where(c => c.Count == maxCount).ToList();
        }
    }

    private string FormatDuration(int months)
    {
        if (months < 12)
        {
            return $"{months} {(months == 1 ? "mês" : "meses")}";
        }

        var years = months / 12;
        var remainingMonths = months % 12;

        if (remainingMonths == 0)
        {
            return $"{years} {(years == 1 ? "ano" : "anos")}";
        }

        return $"{years} {(years == 1 ? "ano" : "anos")} {remainingMonths} {(remainingMonths == 1 ? "mês" : "meses")}";
    }

    private string FormatMonthYear(DateTime date)
    {
        return date.ToString("MMMM yyyy", new System.Globalization.CultureInfo("pt-PT"));
    }
}