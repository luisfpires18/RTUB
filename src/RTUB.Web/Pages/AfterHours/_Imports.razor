@using RTUB.Security
@* The After Hours feature gate. Every component in this folder, and in any folder below it,
   inherits this policy: authenticated user AND AfterHours:Enabled. Do not override it on a page. *@
@attribute [Authorize(Policy = AfterHoursAuthorization.Policy)]
