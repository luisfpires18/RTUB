@inherits LayoutComponentBase
@using Microsoft.AspNetCore.Components.Authorization
@using Microsoft.AspNetCore.Components.Routing
@using Microsoft.AspNetCore.Identity
@using Microsoft.EntityFrameworkCore
@using RTUB.Application.Data
@using RTUB.Application.Helpers
@using RTUB.Application.Extensions
@using RTUB.Application.Interfaces
@using RTUB.Core.Entities
@inject NavigationManager NavigationManager
@inject AuthenticationStateProvider AuthenticationStateProvider
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager
@inject IJSRuntime JSRuntime
@inject RTUB.Web.Services.ProfilePictureUpdateService ProfilePictureUpdateService
@implements IDisposable

<nav class="navbar navbar-expand-lg navbar-dark border-bottom navbar-main">
    <div class="container-xxl rtub-wide px-xxl-4">
        <a class="navbar-brand" href="/">RTUB</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#topNav" aria-controls="topNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="topNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">

                <!-- 1. Música (Public / Member) -->
                <li class="nav-item"><NavLink class="nav-link" href="/music"><i class="bi bi-music-note-beamed"></i> Música</NavLink></li>

                <!-- 2. Pedidos (Public only) -->
                <AuthorizeView>
                    <NotAuthorized>
                        <li class="nav-item"><NavLink class="nav-link" href="/request"><i class="bi bi-envelope"></i> Pedidos</NavLink></li>
                    </NotAuthorized>
                </AuthorizeView>

                <!-- 3. Atuações (Public / Member) -->
                <li class="nav-item"><NavLink class="nav-link" href="/events"><i class="bi bi-calendar-event"></i> Atuações</NavLink></li>

                <AuthorizeView Context="mainAuthContext">
                    <Authorized>
                        <!-- 4. Ensaios (Member) -->
                        <li class="nav-item"><NavLink class="nav-link" href="/rehearsals"><i class="bi bi-music-note-list"></i> Ensaios</NavLink></li>
                    </Authorized>
                </AuthorizeView>

                <!-- 7. Órgãos Sociais (Public only) -->
                <AuthorizeView>
                    <NotAuthorized>
                        <li class="nav-item"><NavLink class="nav-link" href="/roles"><i class="bi bi-bank"></i> Órgãos Sociais</NavLink></li>
                    </NotAuthorized>
                </AuthorizeView>

                <AuthorizeView Context="mainAuthContext2">
                    <Authorized>

                        <!-- 5. Membros (Member Dropdown) -->
                        <li class="nav-item dropdown">
                            <button class="nav-link dropdown-toggle" type="button" id="membersDropdown" data-bs-toggle="dropdown" aria-expanded="false" aria-haspopup="true">
                                <i class="bi bi-people"></i> Membros
                            </button>
                            <ul class="dropdown-menu" aria-labelledby="membersDropdown">
                                <li><NavLink class="dropdown-item" href="/members"><i class="bi bi-people"></i> Membros</NavLink></li>
                                <li><NavLink class="dropdown-item" href="/hierarchy"><i class="bi bi-diagram-3"></i> Hierarquia</NavLink></li>
                                <li><NavLink class="dropdown-item" href="/roles"><i class="bi bi-bank"></i> Órgãos Sociais</NavLink></li>
                                <li><NavLink class="dropdown-item" href="/hall-of-fame"><i class="bi bi-trophy"></i> Hall of Fame</NavLink></li>
                            </ul>
                        </li>

                        <!-- 6. Classificação (Member) -->
                        <li class="nav-item"><NavLink class="nav-link" href="/leaderboard"><i class="bi bi-trophy-fill"></i> Classificação</NavLink></li>
                        
                        <!-- 7. Inventário (Member Dropdown) -->
                        <li class="nav-item dropdown">
                            <button class="nav-link dropdown-toggle" type="button" id="inventoryDropdown" data-bs-toggle="dropdown" aria-expanded="false" aria-haspopup="true">
                                <i class="bi bi-box-seam"></i> Inventário
                            </button>
                            <ul class="dropdown-menu" aria-labelledby="inventoryDropdown">
                                <li><NavLink class="dropdown-item" href="/inventory"><i class="bi bi-music-note"></i> Instrumentos</NavLink></li>
                                <li><NavLink class="dropdown-item" href="/shop"><i class="bi bi-bag"></i> Loja</NavLink></li>
                            </ul>
                        </li>
                        
                        <!-- 8. Gestão (Member Dropdown) -->
                        @if (_shouldShowGestaoDropdown)
                        {
                            <li class="nav-item dropdown">
                                <button class="nav-link dropdown-toggle" type="button" id="managementDropdown" data-bs-toggle="dropdown" aria-expanded="false" aria-haspopup="true">
                                    <i class="bi bi-gear-fill"></i> Gestão
                                </button>
                                <ul class="dropdown-menu" aria-labelledby="managementDropdown">
                                    <li><NavLink class="dropdown-item" href="/meetings"><i class="bi bi-calendar3"></i> Reuniões</NavLink></li>
                                    <li><NavLink class="dropdown-item" href="/documentation"><i class="bi bi-file-earmark-text"></i> Documentação</NavLink></li>
                                    @if (_showBeVisibleToModAdminAndTunoOnly)
                                    {
                                        <li><NavLink class="dropdown-item" href="/logistics"><i class="bi bi-kanban"></i> Logística</NavLink></li>
                                        <li><NavLink class="dropdown-item" href="/finance"><i class="bi bi-cash-stack"></i> Tesouraria</NavLink></li>
                                    }

                                    @if (_showBeVisibleToAdminOnly)
                                    {
                                        <li><NavLink class="dropdown-item" href="/requests"><i class="bi bi-envelope"></i> Pedidos</NavLink></li>
                                    }
                              
                                </ul>
                            </li>
                        }
                    </Authorized>
                </AuthorizeView>

                @if (_showBeVisibleToAdminOnly)
                {
                <AuthorizeView Roles="Admin">
                    <Authorized Context="adminOwnerAuth">
                        <li class="nav-item dropdown">
                            <button class="nav-link dropdown-toggle" type="button" id="ownerDropdown"
                                    data-bs-toggle="dropdown" aria-expanded="false" aria-haspopup="true">
                                <i class="bi bi-tools"></i> Operações
                            </button>
                            <ul class="dropdown-menu" aria-labelledby="ownerDropdown">
                                <li><NavLink class="dropdown-item" href="/emails"><i class="bi bi-envelope"></i> Email</NavLink></li>
                                <li><NavLink class="dropdown-item" href="/notifications"><i class="bi bi-bell"></i> Notificações</NavLink></li>
                                <li><NavLink class="dropdown-item" href="/images"><i class="bi bi-images"></i> Imagens</NavLink></li>
                                <li><NavLink class="dropdown-item" href="/labels"><i class="bi bi-tag"></i> Conteúdo</NavLink></li>
                                <li><hr class="dropdown-divider" /></li>
                
                                <AuthorizeView Roles="Owner">
                                    <Authorized Context="ownerAuth">
                                        <li><NavLink class="dropdown-item" href="/owner/user-roles"><i class="bi bi-people"></i> Funções</NavLink></li>
                                        <li><NavLink class="dropdown-item" href="/owner/tracing"><i class="bi bi-shield-check"></i> Tracing</NavLink></li>
                                        <li><NavLink class="dropdown-item" href="/owner/query"><i class="bi bi-terminal"></i> Query</NavLink></li>
                                    </Authorized>
                                </AuthorizeView>
                            </ul>
                        </li>
                    </Authorized>
                </AuthorizeView>
                }
            </ul>

            <AuthorizeView>
                <Authorized>
                    <ul class="navbar-nav mb-2 mb-lg-0">
                        @if (currentUser != null)
                        {
                            @if (currentUser.IsLeitao())
                            {
                                <li class="nav-item d-flex align-items-center">
                                    <CategoryBadge Category="MemberCategory.Leitao" AdditionalClasses="me-1" />
                                </li>
                            }
                            else if (currentUser.IsCaloiro())
                            {
                                <li class="nav-item d-flex align-items-center">
                                    <CategoryBadge Category="MemberCategory.Caloiro" AdditionalClasses="me-1" />
                                </li>
                            }
                            else if (currentUser.IsTuno())
                            {
                                if (currentUser.Positions.Contains(Position.Magister))
                                {
                                    <li class="nav-item d-flex align-items-center">
                                        <PositionBadge Position="Position.Magister" AdditionalClasses="me-1" />
                                    </li>
                                }
                                else
                                {
                                    <li class="nav-item d-flex align-items-center">
                                        <CategoryBadge Category="MemberCategory.Tuno" AdditionalClasses="me-1" />
                                    </li>
                                }
                            }
                        }
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                @if (currentUser != null)
                                {
                                    <img src="@GetNavbarAvatarUrl()" 
                                         alt="@currentUser.Nickname" 
                                         class="navbar-avatar me-2"
                                         onerror="this.onerror=null; this.src='/images/default-avatar.webp';" />
                                    <span>@currentUser.Nickname</span>
                                }
                            </a>
                            <ul class="dropdown-menu dropdown-menu-end">
                                <li><a class="dropdown-item" href="/profile"><i class="bi bi-person-circle"></i> Meu Perfil</a></li>
                                <li><hr class="dropdown-divider" /></li>
                                <li>
                                    <form method="post" action="/auth/logout" @formname="LogoutForm" class="logout-form">
                                        <button type="submit" class="dropdown-item"><i class="bi bi-box-arrow-right"></i> Sair</button>
                                    </form>
                                </li>
                            </ul>
                        </li>
                    </ul>
                </Authorized>
                <NotAuthorized>
                    <a class="btn" href="/login">Entrar</a>
                </NotAuthorized>
            </AuthorizeView>
        </div>
    </div>
</nav>

<div class="layout d-flex">
    <main id="content" class="content flex-fill content-main">
        <div class="container-xxl rtub-wide py-4 px-xxl-4">
            @Body
        </div>
    </main>
</div>

<footer class="footer mt-auto py-3 footer-main">
    <div class="container-xxl rtub-wide text-center px-xxl-4">
        <p class="footer-text">
            &copy; 2025 Real Tuna Universitária de Bragança.
        </p>
    </div>
</footer>

<HeadContent>
    @* Mobile-first viewport configuration *@
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0, user-scalable=yes" />
    
    @* Standard favicons for desktop browsers *@
    <link rel="icon" type="image/png" sizes="32x32" href="/icons/rtub-logo-32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/icons/rtub-logo-16.png" />
    <link rel="shortcut icon" href="favicon.ico" />
    
    @* Apple Touch Icon for iOS - moved to App.razor for static rendering *@
    
    @* PWA Configuration - manifest link moved to App.razor for static rendering *@
    <meta name="theme-color" content="#3F2A86" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />

    @* Preconnect to CDN for faster loading *@
    <link rel="preconnect" href="https://cdnjs.cloudflare.com" crossorigin>
    <link rel="dns-prefetch" href="https://cdnjs.cloudflare.com">

    <VersionedAsset Path="/lib/bootstrap/bootstrap.min.css" Type="VersionedAsset.AssetType.Css" />
    <VersionedAsset Path="/lib/bootstrap-icons/bootstrap-icons.min.css" Type="VersionedAsset.AssetType.Css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.6.2/cropper.min.css" crossorigin="anonymous" />
    <VersionedAsset Path="/css/site.css" Type="VersionedAsset.AssetType.Css" />
    <VersionedAsset Path="/css/rank-card.css" Type="VersionedAsset.AssetType.Css" />
    <VersionedAsset Path="/css/leaderboard.css" Type="VersionedAsset.AssetType.Css" />
    <VersionedAsset Path="/RTUB.styles.css" Type="VersionedAsset.AssetType.Css" />
    <VersionedAsset Path="/css/member-map.css" Type="VersionedAsset.AssetType.Css" />
    @* Leaflet CSS for Member Map *@
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
          integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY="
          crossorigin="" />
    @* All page and component-specific CSS files are now imported via site.css *@
</HeadContent>

<script src="/_framework/blazor.web.js" autostart="false" suppress-error="BL9992"></script>
<VersionedAsset Path="/lib/bootstrap/bootstrap.bundle.min.js" Type="VersionedAsset.AssetType.Js" />
<script src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.6.2/cropper.min.js" crossorigin="anonymous"></script>
<VersionedAsset Path="/js/fileDownload.js" Type="VersionedAsset.AssetType.Js" />
<VersionedAsset Path="/js/imageCropper.js" Type="VersionedAsset.AssetType.Js" />
<VersionedAsset Path="/js/profilePictureRefresh.js" Type="VersionedAsset.AssetType.Js" />
<VersionedAsset Path="/js/familyTree.js" Type="VersionedAsset.AssetType.Js" />
<VersionedAsset Path="/js/roleBadge.js" Type="VersionedAsset.AssetType.Js" />
<VersionedAsset Path="/js/scrollToTop.js" Type="VersionedAsset.AssetType.Js" />
<VersionedAsset Path="/js/kanban.js" Type="VersionedAsset.AssetType.Js" />
    @* Leaflet JS for Member Map *@
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"
        integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo="
        crossorigin=""></script>
<VersionedAsset Path="/js/memberMap.js" Type="VersionedAsset.AssetType.Js" />
<VersionedAsset Path="/js/push-notifications.js" Type="VersionedAsset.AssetType.Js" />
<script>
    // Configure Blazor to use absolute path for SignalR hub
    // This prevents 404 errors when on /admin/* or other sub-paths
    Blazor.start({
        circuit: {
            configureSignalR: function (builder) {
                builder.withUrl("/_blazor");
            }
        }
    });
</script>

@code {
    private ApplicationUser? currentUser;
    private string? _lastLocation;
    private System.Threading.Timer? _debounceTimer;
    private bool _isFirstRender = true;
    private AuthenticationState? _cachedAuthState;
    private bool _shouldShowGestaoDropdown;
    private bool _showBeVisibleToAdminOnly;
    private bool _showBeVisibleToModAdminAndTunoOnly;

    protected override async Task OnInitializedAsync()
    {
        await LoadCurrentUser();

        // Subscribe to profile picture updates
        ProfilePictureUpdateService.OnProfilePictureUpdated += HandleProfilePictureUpdated;

        // Subscribe to location changes
        NavigationManager.LocationChanged += HandleLocationChanged;
        _lastLocation = NavigationManager.Uri;
    }

    protected override Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            _isFirstRender = false;
        }
        return Task.CompletedTask;
    }

    private void HandleLocationChanged(object? sender, LocationChangedEventArgs e)
    {
        // Debounce to avoid double fires
        _debounceTimer?.Dispose();

        _debounceTimer = new System.Threading.Timer(_ =>
        {
            try
            {
                var newLocation = e.Location;

                // Check if we have a valid previous location
                if (string.IsNullOrEmpty(_lastLocation))
                {
                    _lastLocation = newLocation;
                    return;
                }

                var oldUri = new Uri(_lastLocation);
                var newUri = new Uri(newLocation);

                // Check if it's a hash change only (same path)
                bool isHashChangeOnly = oldUri.GetLeftPart(UriPartial.Path) == newUri.GetLeftPart(UriPartial.Path);

                // Reload user when navigating away from profile page (avatar may have changed)
                if (_lastLocation.Contains("/profile", StringComparison.OrdinalIgnoreCase) && 
                    !newLocation.Contains("/profile", StringComparison.OrdinalIgnoreCase))
                {
                    _ = InvokeAsync(async () =>
                    {
                        await LoadCurrentUser();
                        StateHasChanged();
                    });
                }

                // Only scroll if it's not a hash-only change and not the first render
                if (!isHashChangeOnly && !_isFirstRender)
                {
                    // Fire-and-forget is intentional here; we don't need to wait for scroll completion
                    _ = JSRuntime.InvokeVoidAsync("scrollToTop.scrollInstant");
                }

                _lastLocation = newLocation;
            }
            catch
            {
                // Ignore errors during navigation or disposal
            }
        }, null, 50, Timeout.Infinite);
    }

    private async Task LoadCurrentUser()
    {
        _cachedAuthState = await AuthenticationStateProvider.GetAuthenticationStateAsync();
        var claimsPrincipal = _cachedAuthState.User;

        if (claimsPrincipal?.Identity?.IsAuthenticated == true)
        {
            // Try multiple ways to get the user identifier
            var userName = claimsPrincipal.Identity.Name;
            var nameIdentifierClaim = claimsPrincipal.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value;
            var nameClaim = claimsPrincipal.FindFirst(System.Security.Claims.ClaimTypes.Name)?.Value;

            ApplicationUser? user = null;

            // Try to find by username first
            if (!string.IsNullOrEmpty(userName))
            {
                user = await UserManager.FindByNameAsync(userName);
            }

            // Try by name claim
            if (user == null && !string.IsNullOrEmpty(nameClaim))
            {
                user = await UserManager.FindByNameAsync(nameClaim);
                if (user == null)
                {
                    user = await UserManager.FindByEmailAsync(nameClaim);
                }
            }

            // Try by user ID from NameIdentifier claim
            if (user == null && !string.IsNullOrEmpty(nameIdentifierClaim))
            {
                user = await UserManager.FindByIdAsync(nameIdentifierClaim);
            }

            // Reload the user to ensure we have the latest data from database
            if (user != null)
            {
                // Force Entity Framework to reload from database instead of using cached data
                var entry = UserManager.Users.AsNoTracking().FirstOrDefault(u => u.Id == user.Id);
                currentUser = entry ?? user;
            }
        }
        
        // Update authorization flags after loading user
        await UpdateAuthorizationFlags();
    }
    
    private async Task EnsureCachedAuthStateAsync()
    {
        if (_cachedAuthState == null)
        {
            _cachedAuthState = await AuthenticationStateProvider.GetAuthenticationStateAsync();
        }
    }
    
    private async Task UpdateAuthorizationFlags()
    {
        await EnsureCachedAuthStateAsync();
        
        // Execute authorization checks concurrently for better performance
        var tasks = Task.WhenAll(
            ShouldShowGestaoDropdown(),
            ShowBeVisibleToAdminOnly(),
            ShowBeVisibleToModAdminAndTunoOnly()
        );
        
        var results = await tasks;
        _shouldShowGestaoDropdown = results[0];
        _showBeVisibleToAdminOnly = results[1];
        _showBeVisibleToModAdminAndTunoOnly = results[2];
    }

    private string GetNavbarAvatarUrl()
    {
        if (currentUser == null) return "/images/default-avatar.webp";
        return currentUser.ProfilePictureSrc ?? "/images/default-avatar.webp";
    }

    private void HandleProfilePictureUpdated()
    {
        // Reload user data when profile picture is updated
        _ = InvokeAsync(async () =>
        {
            await LoadCurrentUser();
            StateHasChanged();
        });
    }

    public void Dispose()
    {
        NavigationManager.LocationChanged -= HandleLocationChanged;
        ProfilePictureUpdateService.OnProfilePictureUpdated -= HandleProfilePictureUpdated;
        _debounceTimer?.Dispose();
    }

    /// <summary>
    /// Determines if the "Gestão" dropdown should be shown to the current user.
    /// Visibility rules:
    /// - LEITÃO: HIDE (always)
    /// - CALOIRO + MEMBER: HIDE
    /// - CALOIRO + ADMIN: SHOW
    /// - CALOIRO + MOD: SHOW
    /// - TUNO + any role: SHOW
    /// - Admin: SHOW (always)
    /// - Mod: SHOW (always)
    /// - Owner: SHOW (always)
    /// </summary>
    private async Task<bool> ShouldShowGestaoDropdown()
    {
        if (currentUser == null)
        {
            return false;
        }

        await EnsureCachedAuthStateAsync();
        var user = _cachedAuthState!.User;

        // Admin / Owner / Mod always see it
        if (user.IsInRole("Admin") || user.IsInRole("Owner") || user.IsInRole("Mod"))
        {
            return true;
        }

        // Everyone else except Leitão sees it
        return !currentUser.IsLeitao();
    }


    /// <summary>
    /// Determines if the "Images" menu item should be shown.
    /// Shown to: Admin and Owner.
    /// </summary>
    private async Task<bool> ShowBeVisibleToAdminOnly()
    {
        if (currentUser == null) return false;

        await EnsureCachedAuthStateAsync();
        var isAdmin = _cachedAuthState!.User.IsInRole("Admin");
        
        return isAdmin;
    }

    /// <summary>
    /// Determines if the "Images" menu item should be shown.
    /// Shown to: Admin and Owner.
    /// </summary>
    private async Task<bool> ShowBeVisibleToModAdminAndTunoOnly()
    {
        if (currentUser == null) return false;
        
        await EnsureCachedAuthStateAsync();
        var user = _cachedAuthState!.User;
        var isTuno = currentUser.IsTuno();
        var isMod = _cachedAuthState.User.IsInRole("Mod");
        var isAdmin = _cachedAuthState.User.IsInRole("Admin");
        
        return isAdmin || isMod || isTuno;
    }
}