@namespace RTUB.Shared

@*
    PersonBadge Component
    Displays a person's name in a badge with optional close button
    Matches the simple badge style used on /owner/tracing (bg-primary)
    No avatar - just text and optional close button
*@

<span class="badge bg-primary person-badge @AdditionalClasses">
    @Name
    @if (ShowCloseButton)
    {
        <span role="button" 
              class="person-badge-close" 
              @onclick="OnClose" 
              @onclick:stopPropagation="true"
              aria-label="Remover">×</span>
    }
</span>

@code {
    /// <summary>
    /// The person's display name
    /// </summary>
    [Parameter]
    public string Name { get; set; } = string.Empty;

    /// <summary>
    /// Whether to show the close button
    /// </summary>
    [Parameter]
    public bool ShowCloseButton { get; set; } = false;

    /// <summary>
    /// Additional CSS classes to apply to the badge
    /// </summary>
    [Parameter]
    public string AdditionalClasses { get; set; } = "";

    /// <summary>
    /// Callback when close button is clicked
    /// </summary>
    [Parameter]
    public EventCallback OnClose { get; set; }
}
